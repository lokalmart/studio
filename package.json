{
  "name": "lokalmart-importer-studio",
  "version": "1.3.0",
  "private": true,
  "description": "Lokalmart Importer Studio with superapp horizontal slide UI, record editor, photo upload, and Odoo import engine.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "lint": "node -c api/odoo.js && for f in lib/*.js; do node -c \"$f\"; done"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "xmlrpc": "^1.3.2"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}