{
  "name": "lokalmart-importer-studio",
  "version": "1.2.0",
  "private": true,
  "description": "Lokalmart Importer Studio with mobile-first record editor, local photo upload, Odoo native-like import, and photo retry.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "lint": "node -c api/odoo.js && for f in lib/*.js; do node -c \"$f\"; done"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "xmlrpc": "^1.3.2"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
